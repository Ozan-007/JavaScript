// Window is some kind of a cover for our code. It contains our code also builtin method etc. We can control things and check things which is literally on the window.

let value = confirm("are you sure ? ")
var a = 120
// let val = window.scrollX;

let val = window.location;

console.log(value)
console.log(val)